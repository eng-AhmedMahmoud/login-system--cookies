/*var isAllUsers = JSON.parse(localStorage.getItem("isAllUsers"));
var allUsers = JSON.parse(localStorage.getItem("allusers"));
for(var k = 0; k < isAllUsers.length; k++){

    for(var i = 0; i < allUsers.length; i++){

        if(isAllUsers[k].logEmail == allUsers[i].email && isAllUsers[k].logPassword == allUsers[i].password){
            document.querySelector(".content1").innerHTML = `welcome` + allUsers[i].name;
        }
    
    }
     
} */